<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jayshah.gradecalculatorstylaz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ProfileOrQuickCalc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SchoolTypeSelection" />
        <activity android:name="com.exa.MainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".University_Selection" />
        <activity android:name=".college_selection"></activity>
    </application>

</manifest>